<!-- everything here is working fine but there are little changes, in the invoice table data, when the status is SUBMITTED, i want it to have a background color of yellow with a text of "in progress". when the status is APPROVED, i want it to have a background of pink and a text of APPROVED, when the status is COMPLETED , i want it to have a background of green and a text of COMPLETED .

so in regards to approving invoice, the twist here is this , The status of every invoice is "SUBMITTED" by default in the response of the hook get all staff invoices. two people have access to this invoice page, the ADMIN and the SUPER_ADMIN. now when admin logs in, click that view details that pops up the modal, by default the button text is "approve payment", when admin hit the endpoint "apporveInvoiceAdmin" by clicking the approve payment button and that particular invoice is successfully approved , the status from the hook changes to "APPROVED" and the approve payment button text changes to Admin Approved so the super admin will know  note when this button tetxt changes , the background becomes green and text white. so if super admin logs in and click the view details of that invoice that has already been approved by admin, then they will see the admin approved text and  click on a proceed button   that now hit approveInvoiceSuperAdmin endpoint then if the endpoint is successfull then the payment modal pops. only SUPER_ADMIN can see the payment receipt modal... so if the status of the invoice is APPROVED and an admin click the view view details the button text will be Admin Approved so they kno it has already been approved, the proceed button is not visible to them and cant hit the endpoint again..write full code lets see if we can fix that first

  -->
